// 通報成功時の処理をここに記述
// 例: モーダルを閉じる、成功メッセージを表示するなど
<% if defined?(comment) %>
  $("#modal-<%= comment.id %>").modal("hide"); // モーダルを閉じる
<% end %>
$("#report-success-message").text("通報が成功しました。"); // 成功メッセージを表示
