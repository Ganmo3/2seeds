// 通報成功メッセージを表示
$("#reportSuccessMessage").text("通報が成功しました。");

// モーダルウィンドウを閉じる
$('#report-modal').addClass('fade-out');
setTimeout(() => {
  $('#report-modal').hide().removeClass('fade-out');
}, 300);
